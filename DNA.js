const dna = (string) => {
    // handle only 1 number?
    for (let i in string){
        if( i === i.toLowerCase() ){
            string.slice(i,1);
        }else if( string.length = 0){
            i;
        }
        return i;
    }
}



module.exports = dna;